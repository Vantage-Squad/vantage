// Stub — replaced on Day 3 with the real SSE implementation.
// auth.ts dynamically imports stopSSE on logout, so this file must exist
// to prevent a runtime error before the SSE client is built.

export function stopSSE(): void {
  // No-op until Day 3
}